# 续开发记录 · 2026-07-01



**项目路径**：`f:\obsidian知识库\cursor\dingtalk-reimbursement`



---



## 本次完成（P3 续）



| 项 | 说明 |

|----|------|

| 预算释放 | `releaseBudgetOnReject`：审批通过后驳回/撤回/OA 终止时写入 `budget_ledger`（`biz_type=release`） |

| 已用预算计算 | `getUsedAmount` / 看板汇总改为占用与释放净值 |

| 驳回流程 | 驳回时取消其余待办任务；OA reject/terminate 同步释放 |

| 撤回 | `POST /api/reimbursements/:id/cancel`，审批中申请人可撤回（方案 A 取消实例） |

| 报表 API | `GET /api/reports/summary` 报销状态汇总 |

| API 用量 | `GET /api/api-usage` 当月钉钉 API 调用统计 |

| 前端 | 详情页「撤回报销」、首页入口「统计报表」页 `Reports.vue` |

| 验证 | `verify-p3.js` 增加释放/驳回/撤回用例 |



---



## 本次完成（填单实时提示 + 报表筛选）



| 项 | 说明 |

|----|------|

| 发票查重 API | `GET /api/invoices/check?no=&code=&excludeReimbId=` |

| 预算预览 API | `GET /api/budget/preview?projectId=&expenseDate=&items=`（JSON 数组） |

| 预算预览服务 | `previewBudgetForItems`：按科目聚合，返回 ok/warn/error 级别 |

| 填单页 UI | `ReimbCreate.vue`：发票号 debounce 查重提示、项目/金额/科目预算预览卡片 |

| 报表筛选 | `GET /api/reports/summary` 支持 `dateFrom` / `dateTo`；`Reports.vue` 项目 + 日期筛选 |

| 验证 | `verify-p3.js` 增加 budget preview、报表日期筛选用例 |



---



## 本次完成（报表导出 + 发票 OCR）

| 项 | 说明 |
|----|------|
| 报表导出 API | `GET /api/reports/summary/export?format=csv&projectId=&dateFrom=&dateTo=`，复用 summary 筛选逻辑，UTF-8 BOM CSV |
| 报表页 UI | `Reports.vue` 增加「导出 CSV」按钮（Bearer + blob 下载） |
| 发票 OCR API | `POST /api/invoices/ocr` body `{ fileId }`，本地 MVP：文件名/文件内文本正则识别 |
| 填单页 UI | `ReimbCreate.vue` 上传后自动识别 + 附件「识别发票号」按钮，可指定填入明细行 |
| 第三方 hook | `invoice.verify_enabled=true` 时返回 stub 提示（未接入付费 API）；用量记 `invoice.ocr.external` |
| 验证 | `verify-p3.js` 增加 CSV 导出与 OCR 用例 |

---

## 仍待做

- 发票 OCR 接入真实 OCR/验真 API（当前扫描件图片识别率低）

---

## 本次完成（P5 部署与运维）

| 项 | 说明 |
|----|------|
| Docker 生产 | `docker-compose.prod.yml`、健康检查、`.env.production.example` |
| 裸机部署 | PM2、`deploy/nginx.conf`、非 Docker 指南 |
| 初始化 | `scripts/deploy/init-production.*`、`backend/scripts/seed-prod.js` |
| 备份 | `scripts/backup/mysql-backup.*`、`mysql-restore.*`、`backup-uploads.*` |
| 文档 | `docs/P5-部署说明.md`、`docs/管理员手册.md` |
| 健康检查 | `/api/health` phase=P5 |

---

## 本次完成（P4 OA 对接）

| 项 | 说明 |
|----|------|
| OA 表单 | 字段 label 与模板对齐；附件文件名写入「发票及凭证」 |
| OA 失败回滚 | 发起失败保持草稿，写入 `sync_status=failed` |
| processCode | 启动时从 `DINGTALK_PROCESS_CODE` 同步到 `sys_config` |
| Stream | 修复 `bpms_instance_change` 载荷解析；失败返回 LATER 重试 |
| HTTP Webhook | `POST/GET /api/webhooks/dingtalk/oa`（生产加密 / 开发明文） |
| 配置 API | `GET /api/config/approval` |
| 前端 | 详情页 OA 状态卡片；提交成功 A/B 不同提示 |
| 开发模拟 | 增强 `webhooks/oa-instance`；`scripts/simulate-oa-event.js` |
| 验证 | `npm run verify:p4` |
| 文档 | `docs/P4-OA对接指南.md` |

---



## 快速验证



```powershell

cd "f:\obsidian知识库\cursor\dingtalk-reimbursement\backend"

node scripts/verify-p3.js

```



浏览器：



- 填单实时提示：http://localhost:5173/h5/reimb/create

- 报表筛选 / 导出 CSV：http://localhost:5173/h5/reports

- 发票 OCR：填单页上传名为 `发票号码1234567890.jpg` 的附件，或点「识别发票号」



*更新于 2026-07-01*

