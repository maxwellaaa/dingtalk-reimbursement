# 会话备份 · 钉钉协作报销 · v1.0.0 存档

**保存日期**：2026-07-30  
**项目路径**：`F:\obsidian知识库\cursor\dingtalk-reimbursement`  
**版本**：`1.0.0`（托管就绪）

---

## 本会话要点

| 主题 | 结论 / 成果 |
|------|-------------|
| 员工项目可见性 | 默认不显示；财务/总经理/项目经理分配后才出现 |
| 角色确认 | 钉钉入企默认员工；总经理/财务须「角色分配」或冷启动 SQL |
| 托管平台 | 钉钉不托管自研 Node；推荐阿里云/腾讯云轻量 2C2G + Docker |
| **版本存档** | `VERSION=1.0.0`、`CHANGELOG.md`、`npm run pack:release` → `releases/*.zip` |

---

## 版本与托管交付物

| 文件 | 说明 |
|------|------|
| `VERSION` | `1.0.0` |
| `CHANGELOG.md` | 变更说明 |
| `docs/版本与服务器托管准备-v1.0.0.md` | 上传与上线清单 |
| `scripts/pack-release.ps1` | 排除密钥与 node_modules 后打 zip |
| `releases/dingtalk-reimbursement-1.0.0.zip` | 本机生成的发布包（不入库） |
| `GET /api/health` | 返回 `version: "1.0.0"` |

---

## 服务器下一步

1. `npm run pack:release`
2. 上传 zip → 解压 → 填 `.env`
3. `docker compose -f docker-compose.prod.yml up -d --build`
4. `init-db` + `seed-prod` → 配 HTTPS → 钉钉首页/安全域名
5. 冷启动首位总经理 → 角色分配 → 项目成员

详见 `docs/版本与服务器托管准备-v1.0.0.md`。

---

## 相关历史备份

- [会话备份-2026-07-01.md](./会话备份-2026-07-01.md)（P0–P5 初交付）
